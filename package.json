{
  "scripts": {
    "build": "node build.js",
    "serve": "cd build && serve",
    "dev": "nodemon build.js & cd build; browser-sync start --server"
  },
  "devDependencies": {
    "browser-sync": "^2.18.8",
    "marked": "^0.3.6",
    "nodemon": "^1.11.0",
    "shelljs": "^0.7.6"
  }
}
